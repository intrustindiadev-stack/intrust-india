# Operational SQL Scripts

This folder contains operational scripts: one-off resets, debug queries, and demo data wipes.

**WARNING:** These scripts are dangerous and must NEVER be run against production without a verified backup.

Files in the `dangerous/` subfolder are particularly risky (e.g. wiping user data, hard resets). Use with extreme caution.
