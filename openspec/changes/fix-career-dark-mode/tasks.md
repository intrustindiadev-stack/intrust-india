## 1. Landing Page Dark Mode
- [x] 1.1 In `app/(customer)/career/page.jsx`, update the root container class to include `dark:bg-gray-900`.
- [x] 1.2 Update the "Benefits" section container and elements to use `dark:bg-gray-900`/`dark:bg-gray-800` and `dark:text-white` alongside existing light mode classes.
- [x] 1.3 Update the "Testimonials" section and cards to adapt to dark mode (e.g. `dark:bg-gray-800`, `dark:border-gray-700`).
- [x] 1.4 Update the "Open Roles" cards (and skeleton loaders) with `dark:bg-gray-800`, `dark:border-gray-700`, and `dark:text-white` utility classes.

## 2. Application Flow Dark Mode
- [x] 2.1 In `app/(customer)/career/apply/page.jsx`, update the root container to support `dark:bg-gray-900`.
- [x] 2.2 Update the main multi-step form card to `dark:bg-gray-800`, `dark:border-gray-700`.
- [x] 2.3 Update `InputField` and `SelectField` components to have dark mode backgrounds (`dark:bg-gray-800`), borders (`dark:border-gray-700`), and text (`dark:text-white`).
- [x] 2.4 Update specific step views (Role list, Personal Info inputs, Success and KYC states) to ensure all text elements and icons are legible in dark mode.
