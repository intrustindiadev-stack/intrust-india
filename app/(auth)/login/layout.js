export const metadata = {
    title: "Login",
    description: "Sign in to your InTrust India account to access loans, gift cards, wallet, and more.",
};

export default function LoginLayout({ children }) {
    return children;
}
